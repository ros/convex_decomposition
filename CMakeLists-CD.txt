cmake_minimum_required(VERSION 2.8)
project(convex_decomposition)

file(GLOB_RECURSE CD_SOURCE_CODE ConvexDecomposition/*.cpp)
add_executable(${PROJECT_NAME} DecomposeSample.cpp ${CD_SOURCE_CODE})


message(STATUS "b: DESTDIR=$ENV{DESTDIR}")
message(STATUS "b: CMAKE_SOURCE_DIR=${CMAKE_SOURCE_DIR}")
message(STATUS "b: CMAKE_BINARY_DIR=${CMAKE_BINARY_DIR}")
message(STATUS "b: CATKIN_PROJECT_BIN_DESTINATION=${CATKIN_PROJECT_BIN_DESTINATION}")
message(STATUS "b: CATKIN_BUILD_PREFIX=${CATKIN_BUILD_PREFIX}")

install(TARGETS ${PROJECT_NAME}		
  RUNTIME DESTINATION bin)
